{
  "name": "@eternalos/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --inspector-port=0",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "WRANGLER_LOG_PATH=../../.wrangler-test.log node --test test/**/*.test.mjs",
    "coverage": "WORKER_COVERAGE=1 WRANGLER_LOG_PATH=../../.wrangler-test.log node --test test/**/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "~5.9.3",
    "wrangler": "^3.101.0"
  }
}
