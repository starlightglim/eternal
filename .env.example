# EternalOS Environment Configuration
# Copy this file to .env.local and configure for your environment

# ===================
# Frontend (Vite)
# ===================

# Cloudflare Worker API URL
# Local development: http://localhost:8787
# Production: https://eternalos-api.<your-subdomain>.workers.dev
VITE_API_URL=http://localhost:8787

# ===================
# Worker (wrangler.toml)
# ===================
# Note: Worker secrets are set via `wrangler secret put <NAME>`
#
# Required secrets for production:
#   wrangler secret put JWT_SECRET
#
# KV namespace IDs (run these commands and update wrangler.toml):
#   wrangler kv:namespace create AUTH_KV
#   wrangler kv:namespace create DESKTOP_KV
#
# R2 bucket (create once):
#   wrangler r2 bucket create eternalos-files
